---
description: 初始化项目上下文
---

从 `./.agent/` 加载项目上下文（排除 `./.agent/archive/`）：

1. **`rules/`** — **始终加载** 所有文件（行为准则、技术栈、编码规范、前端架构）
2. **`plans/`** — **不预加载**。仅列出文件名。当前任务相关时加载特定计划。
3. **`workflows/`** — 始终加载所有文件（命令定义）
4. **`references/`** — **不预加载**。仅列出文件名。特定任务需要时加载。
