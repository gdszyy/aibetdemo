/** Password type */
export enum PasswordType {
    /** User password */
    User = 'user',
    /** Wallet password */
    Wallet = 'wallet',
}

/** Password setup mode */
export enum PasswordSetupMode {
    /** Undefined */
    Undefined = 'undefined',
    /** First-time setup */
    First = 'first',
    /** Reset */
    Reset = 'reset',
}
